/** Shown briefly while hydrating saved progress from localStorage. */
export function PersistHint() {
  return (
    <p className="text-sm text-[var(--muted)]">Restoring today&apos;s progress…</p>
  );
}
